server:
   port: 0

spring:
  profiles: test
  datasource:
    url: jdbc:mysql://localhost:11010/sspms?autoReconnect=true&tinyInt1isBit=false&useSSL=false&zeroDateTimeBehavior=convertToNull
    username: root
    password: 'tenxdev'
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
